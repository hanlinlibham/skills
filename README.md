# Skills

Claude Code 自定义技能集合。

## 目录

| 技能 | 说明 |
|------|------|
| [windsdk](windsdk/) | WindPy SDK 本地调用参考手册 — Wind 金融终端 Python API |

## 使用方式

将技能目录复制到 `~/.claude/skills/` 下，Claude Code 会自动识别并加载。

```bash
# 示例：安装 windsdk 技能
cp -r windsdk ~/.claude/skills/windsdk
```
