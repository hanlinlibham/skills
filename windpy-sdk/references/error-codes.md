# WindPy 错误码参考

WindPy 函数返回的错误码及解决方案。

## 错误码速查表

### 成功

| 错误码 | 含义 | 说明 |
|--------|------|------|
| `0` | 成功 | 数据获取成功 |

---

### 数据部分错误码

#### 系统级错误 (-40520001 ~ -40520020)

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| `-40520001` | 未知错误 | 检查 Wind 终端状态，重试 |
| `-40520002` | 内部错误 | 联系 Wind 技术支持 |
| `-40520003` | 系统错误 | 重启 Wind 终端后重试 |
| `-40520004` | 登录失败 | 检查账号密码，或 Wind 终端是否已登录 |
| `-40520005` | 无权限 | 联系 Wind 客服开通数据权限 |
| `-40520006` | 用户取消 | 用户主动取消操作 |
| `-40520007` | 无数据 | 该日期/品种无数据，或数据权限未开通 |
| `-40520008` | 超时错误 | 减少请求数据量，或增加超时时间 |
| `-40520009` | 本地WBOX错误 | 检查 Wind 终端安装 |
| `-40520010` | 需要内容不存在 | 检查代码或字段是否存在 |
| `-40520011` | 需要服务器不存在 | 检查网络连接 |
| `-40520012` | 引用不存在 | 检查引用的代码或字段 |
| `-40520013` | 其他地方登录错误 | 账号在其他地方登录，需重新登录 |
| `-40520014` | 未登录使用WIM工具 | 先登录 Wind 终端 |
| `-40520015` | 连续登录失败次数过多 | 等待一段时间后重试 |

#### 网络/IO错误 (-40521001 ~ -40521011)

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| `-40521001` | IO操作错误 | 检查磁盘空间 |
| `-40521002` | 后台服务器不可用 | 等待服务器恢复 |
| `-40521003` | 网络连接失败 | 检查网络连接 |
| `-40521004` | 请求发送失败 | 检查网络或重试 |
| `-40521005` | 数据接收失败 | 检查网络或重试 |
| `-40521006` | 网络错误 | 检查网络连接 |
| `-40521007` | 服务器拒绝请求 | 联系 Wind 技术支持 |
| `-40521008` | 错误的应答 | 服务器异常，稍后重试 |
| `-40521009` | 数据解码失败 | 数据格式错误，联系技术支持 |
| `-40521010` | 网络超时 | 增加超时时间或减少数据量 |
| `-40521011` | 频繁访问 | 降低请求频率 |

#### 数据服务错误 (-40522001 ~ -40522017)

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| `-40522001` | 无合法会话 | 调用 `w.start()` 建立连接 |
| `-40522002` | 非法数据服务 | 检查数据服务是否可用 |
| `-40522003` | 非法请求 | 检查请求参数格式 |
| `-40522004` | 万得代码语法错误 | 检查代码格式，如 `600519.SH` |
| `-40522005` | 不支持的万得代码 | 该品种不支持此查询 |
| `-40522006` | 指标语法错误 | 检查字段名拼写和格式 |
| `-40522007` | **不支持的指标** | 该字段不适用此品种 |
| `-40522008` | 指标参数语法错误 | 检查参数字段 |
| `-40522009` | 不支持的指标参数 | 该参数不被支持 |
| `-40522010` | 日期与时间语法错误 | 使用 `YYYYMMDD` 或相对日期如 `-30D` |
| `-40522011` | 不支持的日期与时间 | 日期格式错误或超出范围 |
| `-40522012` | 不支持的请求参数 | 检查 options 参数格式 |
| `-40522013` | 数组下标越界 | 请求数据量过大 |
| `-40522014` | 重复的WQID | 请求ID重复 |
| `-40522015` | 请求无相应权限 | 联系 Wind 开通权限 |
| `-40522016` | 不支持的数据类型 | 检查返回数据类型 |
| `-40522017` | 数据提取量超限 | 减少单次请求的数据量 |

---

### 交易部分错误码

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| `-40530001` | 数据错误 | 检查交易参数 |
| `-40530002` | 未初始化 | 调用交易初始化函数 |
| `-40530003` | 功能号错 | 检查功能代码 |
| `-40530004` | 用户已登出或无此LogonID | 重新登录 |
| `-40530005` | 回报队列已满 | 等待或清空队列 |
| `-40530006` | 发送请求失败 | 检查网络连接 |
| `-40530101` | 未通过认证 | 检查认证信息 |
| `-40530201` | 获取配置错误 | 检查配置文件 |
| `-40530202` | 券商代码错 | 检查券商代码 |
| `-40530203` | 登录代码错 | 检查登录代码 |
| `-40530204` | 账号密码错 | 检查账号密码 |
| `-40530205` | 账号类型错 | 检查账号类型 |
| `-40530206` | 登录失败次数过多 | 等待后重试 |
| `-40530301` | 建立连接失败 | 检查网络 |
| `-40530302` | 处理超时 | 增加超时时间 |
| `-40530401` | 交易代码错 | 检查交易代码 |
| `-40530402` | 价格委托方式错 | 检查委托方式 |
| `-40530403` | 委托数量错 | 检查委托数量 |
| `-40530404` | 交易方向错 | 检查买卖方向 |
| `-40530405` | 市场代码错 | 检查市场代码 |
| `-40530406` | 投机套保字段错 | 检查套保标志 |
| `-40530407` | 委托队列满 | 等待后重试 |
| `-40530501` | 撤单委托序号错 | 检查委托序号 |
| `-40530502` | 委托状态不可撤单 | 该状态不能撤单 |
| `-40530601` | 查询过于频繁 | 降低查询频率 |
| `-40530602` | 回报数据已被覆盖 | 及时读取回报 |
| `-40530603` | 获取回报数据超出记录数 | 减少查询数量 |
| `-40530604` | 多于一个的查询条件 | 精简查询条件 |
| `-40530605` | 无对应请求流水号记录 | 检查流水号 |
| `-40530606` | 无对应委托回报 | 检查委托号 |
| `-40530607` | 委托失败 | 检查委托参数 |
| `-40530608` | 委托已发送 | 等待成交回报 |

---

## 常见错误场景

### 1. 字段不支持 (-40522007)

**现象：** 债券指数的 `yield`、`modified_duration` 等字段返回 `-40522007`

**原因：** 该指数没有这个数据字段

**解决：**
```python
# 先用基础字段测试
err, df = w.wsd('CBA00101.CS', 'close', '-30D', '', '', usedf=True)
if err == -40522007:
    print("该字段不支持此品种")
```

### 2. 参数错误 (-40522006 / -40522012)

**现象：** `wset` 等函数返回参数错误

**原因：** 缺少必要参数或参数格式不正确

**解决：**
```python
# 错误 - 缺少 date
w.wset("sectorconstituent", "windcode=000300.SH")

# 正确
w.wset("sectorconstituent", "date=20260108;windcode=000300.SH")
```

### 3. 无数据权限 (-40520005 / -40522015)

**现象：** 实时行情、分钟线等返回权限错误

**解决：**
- 联系 Wind 客服开通对应数据权限
- 使用替代数据（如日级数据替代分钟线）

### 4. 网络/连接错误 (-40521003 / -40522001)

**现象：** 连接失败或无合法会话

**解决：**
```python
# 检查连接状态
if not w.isconnected():
    w.start()

# 或重新连接
w.stop()
w.start()
```

---

## 错误处理示例

### 基本错误处理

```python
from WindPy import w
w.start()

# wsd 错误处理
err, df = w.wsd("600519.SH", "close", "-10D", "", "", usedf=True)
if err == 0:
    print("成功:", df)
else:
    print(f"错误码: {err}")
    if err == -40522007:
        print("字段不支持此品种")
    elif err == -40522006:
        print("指标语法错误，请检查字段名")
    elif err == -40522012:
        print("参数错误，请检查 options 格式")

w.stop()
```

### 批量查询的错误处理

```python
def safe_wsd(code, fields, start, end, options=""):
    """带错误处理的 wsd 查询"""
    err, df = w.wsd(code, fields, start, end, options, usedf=True)
    if err == 0:
        return df
    elif err == -40522007:
        print(f"字段不支持: {fields}")
        return None
    elif err == -40522006:
        print(f"指标语法错误: {fields}")
        return None
    elif err == -40520007:
        print(f"无数据: {code}")
        return None
    else:
        print(f"错误 {err}: {code}")
        return None

# 批量查询
codes = ["600519.SH", "000858.SZ", "000001.SZ"]
for code in codes:
    df = safe_wsd(code, "close", "-10D", "", "")
    if df is not None:
        print(f"{code}: {df['CLOSE'].iloc[-1]}")
```

---

## 调试技巧

### 打印完整错误信息

```python
result = w.wsd("600519.SH", "close", "-10D", "", "")
if result.ErrorCode != 0:
    print(f"错误码: {result.ErrorCode}")
    print(f"错误信息: {result.Data}")  # 有时包含详细错误信息
```

### 分步调试

```python
# 先测试连接
if not w.isconnected():
    w.start()

# 再测试简单查询
result = w.wsd("600519.SH", "close", "-1D", "", "")
print(f"连接测试: {result.ErrorCode}")

# 逐步增加复杂度
result = w.wsd("600519.SH", "close", "-10D", "", "")
print(f"简单查询: {result.ErrorCode}")

result = w.wsd("600519.SH", "close,volume", "-10D", "", "")
print(f"多字段: {result.ErrorCode}")
```

---

## 注意事项

1. **先检查连接：** 调用任何数据函数前确保 `w.isconnected()` 返回 True
2. **小范围测试：** 新字段或新品种先用小日期范围测试
3. **字段拼写：** WindPy 字段名区分大小写，建议全部小写
4. **返回列名：** 使用 `usedf=True` 时，列名自动转为大写
5. **数据权限：** 部分高级数据（如 Tick、分钟线、债券估值字段）需要额外开通
